// Theme
const padding = 5;
const borderRadius = 4;
const borderWidth = 2;
const borderColor = 'rgba(230, 230, 230, 1)';

export const theme: Theme = {
  borderColor,
  borderRadius,
  borderWidth,
  padding,
};
